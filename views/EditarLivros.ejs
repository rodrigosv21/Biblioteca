<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Editar Livros</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
    <style>
      body {
        background: rgb(245, 241, 241);
        background: linear-gradient(
          27deg,
          rgba(245, 241, 241, 1) 25%,
          rgba(131, 137, 139, 1) 80%,
          rgba(0, 0, 0, 0.3225665266106442) 96%
        );
        min-height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
      }
      .fundo {
        background-color: #ffffff75;
        border-radius: 15px;
        padding: 30px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      }
      .form-control,
      .form-select {
        border-radius: 10px;
      }
      .form-control::placeholder {
        color: #000000;
      }
      #button {
        border-radius: 10px;
        background-color: #180f4a;
        color: white;
        width: 100px;
        padding: 10px;
        border: none;
        cursor: pointer;
      }
      #button:hover {
        background: rgb(92, 90, 87);
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="fundo mx-auto">
        <form class="form" action="/editar" method="post">
          <fieldset>
            <legend class="text-center mb-4">Editar Livros</legend>
            <input type="hidden" name="id" value="<%= livros.id %>" />

            <div class="mb-3">
              <input
                type="text"
                name="titulo"
                id="titulo"
                maxlength="15"
                value="<%= livros.titulo %>"
                placeholder="Título"
                class="form-control"
              />
            </div>
            <div class="mb-3">
              <input
                type="text"
                name="autor"
                id="autor"
                maxlength="15"
                value="<%= livros.autor %>"
                placeholder="Autor"
                class="form-control"
              />
            </div>
            <div class="mb-3">
              <select name="generos" id="generos" class="form-select">
                <option value="Narrativo" <%= livros.generos === 'Narrativo' ? 'selected' : '' %>>Narrativo</option>
                <option value="Lirico" <%= livros.generos === 'Lirico' ? 'selected' : '' %>>Lírico</option>
                <option value="Dramático" <%= livros.generos === 'Dramático' ? 'selected' : '' %>>Dramático</option>
                <option value="Ficção" <%= livros.generos === 'Ficção' ? 'selected' : '' %>>Ficção</option>
                <option value="Ciência ficção" <%= livros.generos === 'Ciência ficção' ? 'selected' : '' %>>Ciência Ficção</option>
                <option value="Terror" <%= livros.generos === 'Terror' ? 'selected' : '' %>>Terror</option>
                <option value="Mistério" <%= livros.generos === 'Mistério' ? 'selected' : '' %>>Mistério</option>
                <option value="Romance" <%= livros.generos === 'Romance' ? 'selected' : '' %>>Romance</option>
              </select>
            </div>
            <div class="mb-3">
              <input
                type="number"
                name="npaginas"
                id="npgs"
                maxlength="5"
                value="<%= livros.npaginas %>"
                placeholder="Nº Páginas"
                class="form-control"
              />
            </div>
            <div class="text-center">
              <button id="button" type="submit" class="btn btn-warning" class="btn btn-primary">Alterar</button>
            </div>
          </fieldset>
        </form>
        <div class="text-center mt-3">
          <button class="btn btn-secondary" type="button" onclick="retornar()">Voltar</button>
        </div>
      </div>
    </div>

    <script>
      function retornar() {
        window.location.href = '/registros';
      }
    </script>
  </body>
</html>
